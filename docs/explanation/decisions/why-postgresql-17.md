# Why PostgreSQL 17

This document will explain why PostgreSQL 17 is the chosen database baseline
for TabFlow.

It is intentionally separate from the raw stack decision reference so the
reasoning can stay readable.
