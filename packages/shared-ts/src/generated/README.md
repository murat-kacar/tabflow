Generated contract outputs live here.

Rules:
- treat this directory as machine-written
- do not hand-edit generated files
- keep handwritten shared types in sibling source files under `src/`
- prefer one generator subfolder per source when OpenAPI/Protobuf code arrives

This folder is intentionally committed early so the repo structure is ready
before generated client/server code is introduced.
